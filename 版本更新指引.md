# ARY MVP — 版本更新与团队协作指引

> 最后更新：2026-06-20  
> 适用场景：5 人团队通过 Git 实现版本共享与协同开发

---

## 1. 仓库结构总览

```
togetherGit/                              ← 团队共享根目录
├── AgentDocs/                            ← AI 指引文档（只读参考，不参与代码迭代）
│   ├── 01-ary-project-analysis.md        ← 项目全局分析
│   ├── 02-ary-global-design.md           ← 全局架构设计
│   ├── 03-open-questions.md              ← 待决策问题
│   └── record.md                         ← 历史工作日志
│
├── AGENTS.md                             ← Agent 协作规则
├── README.md                             ← 项目说明
├── PLAN.md                               ← 近期任务窗口
├── STATUS.md                             ← 任务瞬时看板
│
├── docs/                                 ← 业务文档（PRD、领域、IA、权限等）
│
├── design-prototype/                     ← UX 高保真原型
│
├── project/                              ← ★ 核心代码目录
│   ├── public/                           ←   前端页面
│   ├── src/                              ←   后端源码
│   │   ├── app.js                        ←     Express API 服务
│   │   ├── db.js                         ←     SQLite 持久化
│   │   ├── business.js                   ←     业务逻辑层
│   │   ├── auth.js                       ←     鉴权
│   │   ├── stores.js                     ←     存储层
│   │   ├── state-machine.js              ←     状态机
│   │   └── ca-verifier.js                ←     DCR 验签
│   ├── db/                               ←     数据库文件
│   ├── test/                             ←     测试
│   ├── package.json                      ←     依赖配置
│   └── server.js                         ←     静态服务
│
└── packages/                             ← 旧版本参考
    └── dev-1-impl/                       ← 零依赖版 DEV-1 实现
```

---

## 2. Git 协作流程

### 2.1 首次拉取

```bash
# 每个成员克隆仓库
git clone <仓库地址> togetherGit
cd togetherGit

# 安装依赖
cd project
npm install
cd ..
```

### 2.2 日常开发流程

```bash
# 1. 开始工作前：拉取最新代码
git pull origin main

# 2. 创建功能分支（按任务编号命名）
git checkout -b dev/你的名字/功能描述
# 示例：git checkout -b dev/张三/DEV-2-public-site

# 3. 在分支上开发、提交
git add .
git commit -m "DEV-2: 完成首页赛事画廊布局"

# 4. 推送到远程
git push origin dev/张三/DEV-2-public-site

# 5. 在 Git 平台上创建 Pull Request，指定 reviewer
```

### 2.3 分支命名规范

| 类型 | 格式 | 示例 |
| --- | --- | --- |
| 功能开发 | `dev/人名/任务编号-简述` | `dev/张三/DEV-2-public-site` |
| Bug 修复 | `fix/人名/简述` | `dev/李四/fix-login-redirect` |
| 文档更新 | `docs/人名/简述` | `dev/王五/docs-permission-update` |

### 2.4 合并规范

- **禁止直接推送到 `main` 分支**
- 每个 PR 至少 1 人 Review 后方可合并
- 合并前确保 `npm test` 通过

---

## 3. 版本号规则

遵循语义化版本 `主版本.次版本.补丁`：

| 示例 | 含义 |
| --- | --- |
| `v0.1.0` | MVP 初始版本 |
| `v0.2.0` | 新增功能（如完成 DEV-2） |
| `v0.2.1` | Bug 修复或小改动 |

每次发布新版本时：

```bash
# 在 main 分支上打 tag
git tag v0.2.0
git push origin v0.2.0
```

---

## 4. AgentDocs 使用说明

`AgentDocs/` 目录下的文件是 **AI 上下文指引文档**，供 GitHub Copilot / Claude 等 AI 工具读取项目背景：

| 文件 | AI 读取时机 |
| --- | --- |
| `01-ary-project-analysis.md` | 新成员/新 AI 首次接入时，了解项目全貌 |
| `02-ary-global-design.md` | 涉及架构决策、API 设计时参考 |
| `03-open-questions.md` | 遇到待决策问题时查阅当前讨论议题 |
| `record.md` | 了解历史工作进展和已做决策 |

**注意事项**：
- AgentDocs 是参考文档，**不需要随每次代码提交更新**
- 只有当项目范围、架构或技术选型发生重大变化时才更新
- 日常开发只需关注 `project/` 目录下的代码

---

## 5. 各成员职责建议

| 成员 | 建议负责模块 | 涉及任务 |
| --- | --- | --- |
| 成员 A | Public Site 前端 | DEV-2（首页、赛事页、作品页） |
| 成员 B | Console 前端 | DEV-3/DEV-4（登录、报名、评审 UI） |
| 成员 C | CA 接入 & 大屏 | DEV-5/DEV-6（DCR 对接、Live Hall、大屏） |
| 成员 D | 后端核心 API | DEV-3/DEV-4（API 实现、数据库） |
| 成员 E | 报告 & 发布 | DEV-7/REL-1（榜单、报告、部署） |

---

## 6. 常用命令速查

```bash
npm install          # 安装依赖（在 project/ 目录下）
npm start            # 启动开发服务器 → http://localhost:3000
npm test             # 运行 46 条验收测试
node --test          # 同上（Node 20+）
git status           # 查看当前状态
git log --oneline    # 查看提交历史
```
